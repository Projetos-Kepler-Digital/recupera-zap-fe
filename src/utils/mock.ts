// import { faker } from '@faker-js/faker';

// import { Timestamp } from 'firebase/firestore';

// import type { Funnel } from '@/types/Funnel';

// export const mockFunnnel = (): Funnel => {
//   return {
//     id: faker.string.uuid(),

//     createdAt: Timestamp.fromDate(faker.date.recent()),

//     name: faker.person.firstName(),
//     status: faker.helpers.arrayElement(['active', 'suspended']),

//     shoots: [],
//     leads: [],

//     revenue: faker.number.int({ min: 1000, max: 10000 }),
//     recovers: faker.number.int({ min: 0, max: 1000 }),
//     reaches: faker.number.int({ min: 0, max: 1000 }),
//   };
// };
